<?xml version="1.0" encoding="UTF-8"?>
<examples function="group-items">
    <example>
        <description>
            Add an attribute to the root element, providing information about the content.
        </description>
        <code>
            fox "airports-ireland.xml/augment-doc('\*', 'count(\\@iata)', '""""countIataCodes""""', 'att')"
        </code>
    </example>
    <example>
        <description>
            As the preceding example, but also writing an @xml:base into the root element - perhaps for downstream processing.
        </description>
        <code>
            fox "airports-ireland.xml/augment-doc('\*', 'count(\\@iata)', '""""countIataCodes""""', 'att base')"
        </code>
    </example>
    <example>
        <description>
            Add an empty child element to the root element, to be filled by downstream augmentation.
        </description>
        <code>
            fox "airports-ireland.xml/augment-doc('\*', '()', '""""statistics""""', 'first')"
        </code>
    </example>
    <example>
        <description>
            Perform a two-step augmentation, first adding an empty element, then filling it with a data element.
        </description>
        <code>
            fox "airports-ireland.xml/augment-doc('\*', '()', '""""statistics""""', 'first')\augment-doc('\*\statistics', 'count(\\@iata)', '""""countIata""""')
        </code>
    </example>
    <example>
        <description>
            Copy the root child elements of @href targets into the referencing element.
        </description>
        <code>
            fox "airports-ireland.xml/augment-doc('\\airport[@href]', '@href\resolve-uri(., base-uri(.))/doc(.)\*\*')"
        </code>
    </example>
    <example>
        <description>
            As the previous example, but the new content is wrapped in an element "importedAirportData".
        </description>
        <code>
            fox "airports-ireland.xml/augment-doc('\\airport[@href]', '@href\resolve-uri(., base-uri(.))/doc(.)\*\*', '""""importedAirportData""""')"
        </code>
    </example>
    <example>
        <description>
            Expand a ditamap, inserting topics a first child of the topicref.
        </description>
        <code>
            fox "*.ditamap/augment-doc('\\topicref]', '@href\resolve-uri(., base-uri(.))/doc(.)\*', (), 'first')"
        </code>
    </example>
</examples>
