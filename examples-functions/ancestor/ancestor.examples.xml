<?xml version="1.0" encoding="UTF-8"?>
<function-usage function="ancestor">
    <testenviron> In order to try out the examples, start a command shell and move to folder
        $FOXPATH/examples-data/oxygen-samples. </testenviron>
    <examples>        
        <example label="basic example">
            <description>
                Inspecting a set of docbook documents - which elements contain 'para' elements?
            </description>
            <code>
                fox "docbook//*.xml\\docbook:para\ancestor()\name() => f()"
            </code>
        </example>
        <example label="using a name filter">
            <description>
                Which section related elements contain 'para' elements?
            </description>
            <code>
                fox "docbook//*.xml\\docbook:para\ancestor('sect*')\name() => f()"
            </code>
        </example>
        <example label="using a positional filter">
            <description>
                Which elements contain an element containing a 'para' element?
            </description>
            <code>
                fox "docbook//*.xml\\docbook:para\ancestor((), 2)\name() => f()"
            </code>
        </example>
        <example label="using a more complex name filter">
            <description>
                Inspecting a set of dita documents, which elements contain 'step' elements,
                ignoring elements with a name matching *steps* or *task*?
            </description>
            <code>
                fox "dita//*.dita\\step\ancestor('~*steps* ~*task*')\name() => f()"
            </code>
        </example>
        <example label="Considering only top-level ancestor element">
            <description>
                Inspecting a set of dita documents, which top-level elements (= child elements of
                the root element) contain a 'step' element? Note the wildcard name test, which
                ensures that only element ancestors are considered, so that position -2 means
                a top-level element, rather than a root element.
            </description>
            <code>
                fox "dita//*.dita\\step\ancestor('*', -2)\name() => f()"
            </code>
        </example>
    </examples>
</function-usage>
