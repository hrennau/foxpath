<?xml version="1.0" encoding="UTF-8"?>
<examples function="node-deep-equal">
    <example>
        <description>
            List the names and frequencies of recursive elements - elements which have a descendant with the same name.
        </description>
        <code>
            fox ".//*.dita\\*[descendant(local-name())]\name() => f()"
        </code>
    </example>
    <example>
        <description>
            List files with "root recursion", that is, a root element name which is also used within the document, e.g. a dita subtopic.
        </description>
        <call>
            fox ".//*.dita[\*\descendant(local-name(.))]"        
        </call>
    </example>
    <example>
        <description>
            List all element names containing the substring "title", along with their frequencies.
        </description>
        <call>
            fox ".//*.dita/descendant('*title*')\name() => f()"
        </call>
    </example>
    <example>
        <description>
            List all element names containing the substring "code", along with their frequencies.
        </description>
        <call>
            fox ".//*.dita/descendant('*code*')\name() => f()"
        </call>
    </example>
</examples>
