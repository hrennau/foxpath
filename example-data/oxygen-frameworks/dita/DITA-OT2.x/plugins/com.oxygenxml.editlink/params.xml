<?xml version="1.0" encoding="UTF-8"?>
<extension>
    <!-- pass the edit link as both an XSLT param and a system property. -->
    <param name="editlink.remote.ditamap.url" expression="${editlink.remote.ditamap.url}" if="editlink.remote.ditamap.url"/>
    <!--<sysproperty key="editlink.remote.ditamap.url" value="${editlink.remote.ditamap.url}"/>-->

    <param name="editlink.web.author.url" expression="${editlink.web.author.url}" if="editlink.web.author.url" />

    <param name="editlink.local.ditamap.path" expression="${args.input}"/>
</extension>

